# Toy Dataset
This folder contains `toy_dataset.npz`, generated using `sklearn.datasets.make_regression`.
It is a sparse linear regression dataset with 500 samples, 100 features, and only 10 informative features.
Used in Tasks 2 and 3.
